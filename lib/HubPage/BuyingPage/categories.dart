import 'package:flutter/material.dart';
import 'package:projectBiyt/HubPage/BuyingPage/tabs.dart';

class Buying extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Flexible(child: Example())],
      ),
    );
  }
}
